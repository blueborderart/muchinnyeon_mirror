### Convert to PDF using XeTeX

`xetex Muchinnyeon.tex`

Easiest way would be installing TeX Live. Check the `preamble.tex` to see what packages are needed.

### Convert to EPUB using Pandoc

`pandoc -o Muchinnyeon.epub Muchinnyeon.tex`
